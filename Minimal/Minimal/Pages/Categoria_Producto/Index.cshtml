@page
@model Minimal.Pages.Categoria_Producto.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CATEGORIA_PRODUCTO[0].CAT_ID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CATEGORIA_PRODUCTO[0].PRD_ID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.CATEGORIA_PRODUCTO) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CAT_ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PRD_ID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.CAT_ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.CAT_ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.CAT_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
