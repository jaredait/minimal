@page
@model Minimal.Pages.Carrito.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>CARRITO</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="CARRITO.CLI_USUARIO" class="control-label"></label>
                <input asp-for="CARRITO.CLI_USUARIO" class="form-control" />
                <span asp-validation-for="CARRITO.CLI_USUARIO" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CARRITO.PRD_ID" class="control-label"></label>
                <input asp-for="CARRITO.PRD_ID" class="form-control" />
                <span asp-validation-for="CARRITO.PRD_ID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CARRITO.CAR_CANTIDAD" class="control-label"></label>
                <input asp-for="CARRITO.CAR_CANTIDAD" class="form-control" />
                <span asp-validation-for="CARRITO.CAR_CANTIDAD" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
